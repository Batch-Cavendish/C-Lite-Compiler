int factorial(int n) {
    if (n <= 1) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}

int main() {
    printInt(factorial(5)); // Should print 120
    
    int i = 0;
    while (i < 3) {
        printInt(i);
        i = i + 1;
    }
    return 0;
}